# Secrets — never commit
.env

# Generated run artifacts (repo-root only — must NOT match web/app/runs/, the
# Next.js run-detail route, which an unanchored `runs/` pattern silently ignored)
/runs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/

# OS / editor
.DS_Store
.idea/
.vscode/

# Playwright MCP scratch
.playwright-mcp/
