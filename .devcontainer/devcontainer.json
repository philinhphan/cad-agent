{
  "name": "3d",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:20",
  "containerEnv": {
    "LANG": "de_DE.UTF-8",
    "LC_ALL": "de_DE.UTF-8"
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgl1 libglx-mesa0 && curl -fsSL https://claude.ai/install.sh | bash && if [ -n \"$CLAUDE_JSON\" ]; then printf '%s' \"$CLAUDE_JSON\" > ~/.claude.json; fi"
}